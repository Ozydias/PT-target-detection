# 附录6 操作手册

## 1. 系统概述

### 1.1 系统简介
本系统是基于YOLOv12的目标检测模型优化项目，提供完整的数据处理、模型训练、验证和应用功能。

### 1.2 主要功能
- COCO格式数据转换为YOLO格式
- 数据集验证和修复
- 模型训练和优化
- 模型验证和性能评估
- Web界面进行目标检测

### 1.3 系统要求
- **硬件要求**：
  - GPU：NVIDIA GPU，8GB以上显存（推荐）
  - CPU：多核处理器
  - 内存：16GB以上
  - 存储：至少50GB可用空间
- **软件要求**：
  - 操作系统：Windows 10/11 或 Linux
  - Python：3.11
  - CUDA：11.x（如果使用GPU）

## 2. 环境安装

### 2.1 Python环境安装

#### Windows系统
1. 下载Python 3.11安装包：https://www.python.org/downloads/
2. 运行安装程序，勾选"Add Python to PATH"
3. 完成安装

#### Linux系统
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3.11 python3.11-pip

# CentOS/RHEL
sudo yum install python3.11 python3.11-pip
```

### 2.2 依赖库安装

#### 方法一：使用requirements.txt（推荐）
```bash
# 进入项目目录
cd yolov12-main

# 安装依赖
pip install -r requirements.txt
```

#### 方法二：手动安装
```bash
pip install torch==2.2.2 torchvision==0.17.2
pip install ultralytics
pip install gradio==4.44.1
pip install opencv-python==4.9.0.80
pip install pyyaml==6.0.1
pip install numpy==1.26.4
```

### 2.3 Flash Attention安装（可选，用于加速）
```bash
# Linux系统
wget https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.3/flash_attn-2.7.3+cu11torch2.2cxx11abiFALSE-cp311-cp311-linux_x86_64.whl
pip install flash_attn-2.7.3+cu11torch2.2cxx11abiFALSE-cp311-cp311-linux_x86_64.whl
```

### 2.4 验证安装
```bash
python -c "import torch; print(torch.__version__)"
python -c "import ultralytics; print(ultralytics.__version__)"
python -c "import gradio; print(gradio.__version__)"
```

## 3. 数据集准备

### 3.1 数据集结构

确保数据集按以下结构组织：
```
PTDataset/
├── train/              # 训练图像目录
│   ├── image1.jpg
│   ├── image2.jpg
│   └── ...
├── val/               # 验证图像目录
│   ├── image1.jpg
│   ├── image2.jpg
│   └── ...
├── labels/            # 标签目录
│   ├── train/         # 训练标签（YOLO格式）
│   │   ├── image1.txt
│   │   ├── image2.txt
│   │   └── ...
│   └── val/           # 验证标签（YOLO格式）
│       ├── image1.txt
│       ├── image2.txt
│       └── ...
└── annonations/       # COCO格式标注（可选）
    ├── instances_train.json
    └── instances_val.json
```

### 3.2 COCO格式转YOLO格式

如果您的数据是COCO格式，需要先转换：

```bash
python convert_coco_to_yolo.py --dataset_dir PTDataset
```

**参数说明**：
- `--dataset_dir`：数据集根目录（默认：PTDataset）

**转换结果**：
- 在`PTDataset/labels/train/`目录生成训练标签文件
- 在`PTDataset/labels/val/`目录生成验证标签文件

### 3.3 YOLO格式标签文件说明

每个标签文件（.txt）的格式：
```
class_id center_x center_y width height
```

**示例**：
```
0 0.417187 0.409722 0.062500 0.119444
```

**说明**：
- `class_id`：类别ID（整数，从0开始）
- `center_x, center_y`：归一化中心坐标（0-1之间）
- `width, height`：归一化宽高（0-1之间）

### 3.4 数据集验证

在训练前，建议先验证数据集：

```bash
python check_dataset.py
```

**检查内容**：
- 数据集目录结构
- 图像和标签文件匹配
- 标签文件格式
- 类别ID有效性

**常见问题**：
- 如果发现标签目录中有图像文件，需要手动删除
- 如果发现标签文件格式错误，需要修正

## 4. 模型训练

### 4.1 快速开始

#### 基本训练命令
```bash
python train_yolov12s.py
```

训练脚本会自动：
1. 检测数据集路径
2. 更新配置文件
3. 验证数据集
4. 清理缓存
5. 开始训练

### 4.2 训练参数说明

训练参数在`train_yolov12s.py`中配置，主要参数：

| 参数 | 默认值 | 说明 | 建议值 |
|------|--------|------|--------|
| epochs | 200 | 训练轮数 | 100-300 |
| batch | 16 | 批次大小 | 根据GPU内存调整 |
| imgsz | 640 | 输入图像尺寸 | 640 |
| device | '0' | GPU设备 | '0'或'0,1,2,3' |
| lr0 | 0.01 | 初始学习率 | 0.01 |
| lrf | 0.1 | 最终学习率系数 | 0.1 |
| scale | 0.9 | 数据增强参数 | 0.9（YOLOv12s推荐） |
| mixup | 0.05 | Mixup数据增强 | 0.05（YOLOv12s推荐） |
| copy_paste | 0.15 | Copy-paste数据增强 | 0.15（YOLOv12s推荐） |

### 4.3 修改训练参数

编辑`train_yolov12s.py`文件，找到`model.train()`调用，修改相应参数：

```python
results = model.train(
    data=str(yaml_path),
    epochs=200,          # 修改训练轮数
    batch=16,            # 修改批次大小
    imgsz=640,           # 修改图像尺寸
    device='0',           # 修改GPU设备
    # ... 其他参数
)
```

### 4.4 训练结果

训练完成后，结果保存在：
```
runs/detect/yolov12s_pt_v5:BiFPN/
├── weights/
│   ├── best.pt      # 最佳模型权重
│   └── last.pt      # 最后一个epoch的权重
├── results.png      # 训练曲线图
├── confusion_matrix.png  # 混淆矩阵
├── results.csv      # 训练日志（CSV格式）
└── ...
```

### 4.5 训练问题排查

#### 问题1：训练损失为0
**症状**：训练开始时所有损失值都为0

**解决方法**：
1. 运行`python check_dataset.py`检查数据集
2. 确保标签文件格式正确
3. 删除缓存文件：`PTDataset/train.cache`, `PTDataset/val.cache`
4. 重新运行训练

#### 问题2：CUDA out of memory
**症状**：GPU内存不足错误

**解决方法**：
1. 减小批次大小（batch）：从16改为8或4
2. 减小图像尺寸（imgsz）：从640改为512或416
3. 关闭其他占用GPU的程序

#### 问题3：训练速度慢
**解决方法**：
1. 使用多GPU训练：`device='0,1,2,3'`
2. 增加批次大小（如果GPU内存允许）
3. 使用混合精度训练（已默认开启：`amp=True`）

## 5. 模型验证

### 5.1 验证模型结构

#### 验证P2检测层
```bash
python final_verify_p2.py
```

**输出示例**：
```
✓ 检测层数量: 4 (P2, P3, P4, P5)
✓ P2 stride: 4.0 (最小，分辨率最高)
✓ 模型可以正常前向传播
```

### 5.2 评估模型性能

#### 使用训练脚本评估
训练脚本在训练完成后会自动评估：
```python
metrics = model.val()
print(f"验证集 mAP50: {metrics.box.map50:.4f}")
print(f"验证集 mAP50-95: {metrics.box.map:.4f}")
```

#### 单独评估模型
```python
from ultralytics import YOLO

# 加载训练好的模型
model = YOLO('runs/detect/yolov12s_pt_v5:BiFPN/weights/best.pt')

# 在验证集上评估
metrics = model.val(data='PTDataset.yaml')

# 查看结果
print(f"mAP50: {metrics.box.map50:.4f}")
print(f"mAP50-95: {metrics.box.map:.4f}")
```

## 6. Web应用使用

### 6.1 启动Web应用

```bash
python app.py
```

启动后，在浏览器中访问：
```
http://127.0.0.1:7860
```

### 6.2 使用图像检测

1. **选择输入类型**：选择"Image"
2. **上传图像**：点击图像上传区域，选择要检测的图像
3. **选择模型**：从下拉菜单选择模型
   - 预训练模型：yolov12n.pt, yolov12s.pt等
   - 自定义模型：runs/detect/.../weights/best.pt
4. **调整参数**：
   - **图像尺寸**：320-1280（步长32），默认640
   - **置信度阈值**：0.0-1.0，默认0.1
     - 如果看不到检测框，尝试降低阈值
5. **执行检测**：点击"Detect Objects"按钮
6. **查看结果**：在右侧查看检测结果图像

### 6.3 使用视频检测

1. **选择输入类型**：选择"Video"
2. **上传视频**：点击视频上传区域，选择要检测的视频
3. **选择模型和参数**：同图像检测
4. **执行检测**：点击"Detect Objects"按钮
5. **下载结果**：检测完成后，可以下载处理后的视频

### 6.4 使用示例图像

Web界面提供了示例图像，可以直接点击使用，快速测试模型效果。

### 6.5 常见问题

#### 问题1：看不到检测框
**解决方法**：
1. 降低置信度阈值（如从0.25改为0.1）
2. 检查图像中是否包含目标
3. 尝试使用不同的模型

#### 问题2：检测速度慢
**解决方法**：
1. 减小图像尺寸
2. 使用较小的模型（如yolov12n.pt）
3. 确保使用GPU（检查CUDA是否可用）

#### 问题3：Web界面无法访问
**解决方法**：
1. 检查防火墙设置
2. 检查端口7860是否被占用
3. 尝试使用其他端口：修改`app.py`中的`launch(server_port=7861)`

## 7. 命令行使用

### 7.1 使用Ultralytics CLI

#### 训练模型
```bash
yolo train model=yolov12s.yaml data=PTDataset.yaml epochs=200 batch=16 imgsz=640 device=0
```

#### 验证模型
```bash
yolo val model=runs/detect/yolov12s_pt/weights/best.pt data=PTDataset.yaml
```

#### 预测图像
```bash
yolo predict model=runs/detect/yolov12s_pt/weights/best.pt source=path/to/image.jpg
```

#### 导出模型
```bash
yolo export model=runs/detect/yolov12s_pt/weights/best.pt format=onnx
```

### 7.2 Python API使用

#### 加载模型
```python
from ultralytics import YOLO

# 加载预训练模型
model = YOLO('yolov12s.pt')

# 加载自定义模型
model = YOLO('runs/detect/yolov12s_pt/weights/best.pt')
```

#### 训练模型
```python
results = model.train(
    data='PTDataset.yaml',
    epochs=200,
    batch=16,
    imgsz=640
)
```

#### 验证模型
```python
metrics = model.val(data='PTDataset.yaml')
print(f"mAP50: {metrics.box.map50:.4f}")
```

#### 预测图像
```python
results = model('path/to/image.jpg')
results[0].show()  # 显示结果
results[0].save('output.jpg')  # 保存结果
```

## 8. 配置文件说明

### 8.1 数据集配置文件（PTDataset.yaml）

```yaml
path: c:\Users\29199\Desktop\yolov12-main\PTDataset
train: train
val: val
nc: 1
names:
  0: PT
```

**字段说明**：
- `path`：数据集根目录的绝对路径
- `train`：训练图像目录名（相对于path）
- `val`：验证图像目录名（相对于path）
- `nc`：类别数量
- `names`：类别名称字典

**修改说明**：
- 如果数据集路径改变，修改`path`字段
- 如果类别数改变，修改`nc`和`names`字段

### 8.2 模型配置文件

模型配置文件位于：`ultralytics/cfg/models/v12/yolov12.yaml`

**主要结构**：
- `backbone`：骨干网络
- `neck`：特征融合网络（包含BiFPN）
- `head`：检测头（包含P2, P3, P4, P5检测层）

**修改说明**：
- 一般不需要修改，除非进行模型架构改进
- 修改后需要重新验证模型结构

## 9. 常见问题解答

### Q1: 如何修改检测类别数？
A: 修改`PTDataset.yaml`文件中的`nc`和`names`字段，然后重新训练模型。

### Q2: 训练需要多长时间？
A: 取决于数据集大小、GPU性能和训练轮数。在8GB GPU上，1515张图像训练200轮大约需要50-70小时。

### Q3: 如何提高检测精度？
A: 
1. 增加训练轮数
2. 使用更大的模型（如yolov12m, yolov12l）
3. 增加数据增强
4. 使用更大的输入图像尺寸

### Q4: 如何加快训练速度？
A:
1. 使用多GPU训练
2. 增加批次大小（如果GPU内存允许）
3. 使用混合精度训练（已默认开启）
4. 减小输入图像尺寸

### Q5: 模型可以部署到移动设备吗？
A: 可以，但需要：
1. 导出为ONNX或TensorRT格式
2. 进行模型量化
3. 使用相应的推理引擎

### Q6: 如何保存训练中间结果？
A: 训练脚本会自动保存：
- 每个epoch的检查点（如果设置`save_period`）
- 最佳模型权重（`best.pt`）
- 最后一个epoch的权重（`last.pt`）

## 10. 技术支持

### 10.1 获取帮助
- 查看项目README.md
- 查看训练说明.md
- 查看训练问题修复说明.md
- 检查代码注释

### 10.2 报告问题
如果遇到问题，请提供：
1. 错误信息（完整堆栈跟踪）
2. 运行环境（Python版本、PyTorch版本等）
3. 操作步骤
4. 相关日志文件

---

**文档版本**：v1.0  
**创建日期**：2025年  
**最后更新**：2025年
