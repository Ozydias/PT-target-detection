# 附录2 软件需求说明书

## 1. 引言

### 1.1 编写目的
本文档旨在详细说明基于YOLOv12的目标检测模型优化项目的功能需求、性能需求、接口需求等，为系统设计、开发、测试提供依据。

### 1.2 项目背景
本项目基于YOLOv12目标检测框架，针对特定目标（PT）进行检测模型的训练与优化。项目需要完成从数据处理到模型训练、验证、应用的完整流程。

### 1.3 定义与缩略语
- **YOLOv12**：You Only Look Once version 12，最新的YOLO目标检测模型
- **PT**：项目中的特定检测目标类别
- **COCO**：Common Objects in Context，一种目标检测数据集格式
- **YOLO格式**：YOLO训练所需的标注格式
- **P2层**：小目标检测层，对应更高分辨率的特征图
- **BiFPN**：Bidirectional Feature Pyramid Network，双向特征金字塔网络
- **mAP**：mean Average Precision，平均精度均值
- **FPN**：Feature Pyramid Network，特征金字塔网络

### 1.4 参考资料
- YOLOv12论文：YOLOv12: Attention-Centric Real-Time Object Detectors
- Ultralytics YOLO官方文档
- COCO数据集格式规范
- YOLO训练格式规范

## 2. 项目概述

### 2.1 项目目标
开发一个基于YOLOv12的目标检测系统，能够：
1. 处理COCO格式数据集，转换为YOLO训练格式
2. 在YOLOv12s基础上进行模型改进（P2检测层、BiFPN等）
3. 提供自动化训练流程
4. 支持模型验证和性能评估
5. 提供Web界面进行模型推理

### 2.2 用户特点
- **主要用户**：项目开发人员、研究人员
- **用户技能**：具备Python编程基础，了解深度学习基本概念
- **使用环境**：支持CUDA的GPU服务器，Python开发环境

### 2.3 运行环境
- **硬件环境**：
  - GPU：支持CUDA的NVIDIA GPU（建议8GB以上显存）
  - CPU：多核处理器
  - 内存：建议16GB以上
  - 存储：至少50GB可用空间
- **软件环境**：
  - 操作系统：Windows 10/11 或 Linux
  - Python：3.11
  - PyTorch：2.2.2
  - CUDA：11.x
  - 其他依赖：见requirements.txt

## 3. 功能需求

### 3.1 数据格式转换功能

#### 3.1.1 需求描述
系统需要能够将COCO格式的标注文件转换为YOLO训练所需的格式。

#### 3.1.2 输入
- COCO格式的JSON标注文件（`instances_train.json`、`instances_val.json`）
- 图像文件（JPG/PNG格式）

#### 3.1.3 处理
- 读取COCO格式的JSON文件
- 解析图像信息和标注信息
- 将绝对坐标转换为归一化坐标
- 建立图像ID到文件名的映射
- 生成YOLO格式的标签文件（.txt）

#### 3.1.4 输出
- YOLO格式的标签文件（每行格式：`class_id center_x center_y width height`）
- 标签文件保存在`labels/train/`和`labels/val/`目录

#### 3.1.5 性能要求
- 转换1515个训练图像和171个验证图像应在5分钟内完成
- 支持处理包含多个目标类别的数据集

#### 3.1.6 异常处理
- 处理坐标越界情况
- 处理空标注情况
- 处理图像文件缺失情况
- 提供详细的错误提示

### 3.2 数据集验证功能

#### 3.2.1 需求描述
系统需要能够验证数据集的完整性和正确性。

#### 3.2.2 功能点
- 检查数据集目录结构
- 验证图像文件和标签文件的匹配
- 检查标签文件格式
- 验证类别ID的有效性
- 检查空标签文件
- 检测标签目录中的异常文件（如误存的图像文件）

#### 3.2.3 输出
- 详细的诊断报告
- 问题列表和修复建议

### 3.3 模型改进功能

#### 3.3.1 P2检测层集成
- **需求描述**：在YOLOv12s模型中添加P2检测层，提升小目标检测能力
- **技术要求**：
  - 修改模型配置文件，添加P2层的前向传播路径
  - 调整特征金字塔网络结构
  - 确保P2层特征正确传递到检测头
  - P2层stride应为最小（表示分辨率最高）

#### 3.3.2 BiFPN特征融合网络集成
- **需求描述**：集成BiFPN结构，优化多尺度特征融合
- **技术要求**：
  - 在YOLOv12的neck部分集成BiFPN模块
  - 实现双向特征融合
  - 调整特征融合权重

#### 3.3.3 注意力机制实验
- **需求描述**：探索CBAM和ECA注意力模块的应用
- **技术要求**：
  - 支持在模型中添加注意力模块
  - 可配置注意力模块的位置和数量
  - 支持训练稳定性验证

### 3.4 模型训练功能

#### 3.4.1 需求描述
系统需要提供完整的模型训练功能，支持自动化训练流程。

#### 3.4.2 功能点
- **数据集自动配置**：
  - 自动检测数据集路径
  - 更新YAML配置文件
  - 验证数据集完整性
- **训练参数配置**：
  - 支持自定义训练轮数、批次大小、学习率等
  - 支持YOLOv12s推荐参数（scale=0.9, mixup=0.05, copy_paste=0.15）
  - 支持单类别检测优化参数
- **训练过程管理**：
  - 自动清理缓存文件
  - 支持从检查点恢复训练
  - 实时显示训练进度和指标
- **模型保存**：
  - 自动保存最佳模型权重
  - 保存最后一个epoch的权重
  - 保存训练曲线和验证结果

#### 3.4.3 训练参数
- epochs：训练轮数（默认200，建议100-300）
- batch：批次大小（默认16，根据GPU内存调整）
- imgsz：输入图像尺寸（默认640）
- device：GPU设备（支持单GPU或多GPU）
- 学习率：lr0=0.01, lrf=0.1
- 数据增强：scale=0.9, mixup=0.05, copy_paste=0.15, mosaic=1.0
- 损失权重：box=7.5, cls=0.5, dfl=1.5

### 3.5 模型验证功能

#### 3.5.1 需求描述
系统需要能够验证模型结构和训练效果。

#### 3.5.2 功能点
- **模型结构验证**：
  - 验证P2检测层配置
  - 检查检测层数量、stride、通道数
  - 验证模型前向传播正确性
- **性能评估**：
  - 计算mAP50和mAP50-95指标
  - 生成混淆矩阵
  - 绘制训练曲线
  - 保存验证结果图像

### 3.6 Web应用功能

#### 3.6.1 需求描述
系统需要提供Web界面，支持图像和视频的目标检测。

#### 3.6.2 功能点
- **模型选择**：
  - 支持选择预训练模型（yolov12n/s/m/l/x）
  - 支持选择训练好的自定义模型
- **输入处理**：
  - 支持图像上传和检测
  - 支持视频上传和检测
  - 支持示例图像快速测试
- **参数配置**：
  - 可调整图像尺寸（320-1280，步长32）
  - 可调整置信度阈值（0.0-1.0）
- **结果展示**：
  - 显示检测结果图像（带边界框和标签）
  - 显示检测到的目标数量
  - 支持结果下载

## 4. 性能需求

### 4.1 训练性能
- 训练速度：在单GPU（8GB显存）上，批次大小为16时，每个epoch应在10-30分钟内完成
- 内存使用：训练时GPU内存占用不超过GPU显存的90%
- 训练稳定性：训练损失应正常下降，不应出现NaN或异常波动

### 4.2 推理性能
- 图像检测：单张640x640图像检测时间应小于100ms（在T4 GPU上）
- 视频检测：支持实时视频检测（取决于视频分辨率和帧率）
- Web响应：Web界面响应时间应小于3秒

### 4.3 模型性能
- 检测精度：mAP50应达到40%以上（在验证集上）
- 检测速度：在T4 GPU上，TensorRT优化后推理时间应小于3ms（YOLOv12s）

## 5. 接口需求

### 5.1 用户接口
- **命令行接口**：
  - 数据转换：`python convert_coco_to_yolo.py --dataset_dir PTDataset`
  - 数据集检查：`python check_dataset.py`
  - 模型训练：`python train_yolov12s.py`
  - Web应用：`python app.py`
- **Web界面**：
  - 基于Gradio的图形化界面
  - 支持图像/视频上传
  - 实时显示检测结果

### 5.2 软件接口
- **Python API**：
  - Ultralytics YOLO库接口
  - PyTorch接口
  - NumPy接口
- **文件接口**：
  - YAML配置文件（数据集配置、模型配置）
  - JSON文件（COCO格式标注）
  - TXT文件（YOLO格式标签）

### 5.3 硬件接口
- GPU接口：通过CUDA访问NVIDIA GPU
- 存储接口：文件系统读写

## 6. 数据需求

### 6.1 输入数据
- **训练数据**：
  - 图像文件：1515张训练图像（JPG/PNG格式）
  - 标注文件：COCO格式JSON或YOLO格式TXT
- **验证数据**：
  - 图像文件：171张验证图像
  - 标注文件：对应格式的标注文件

### 6.2 输出数据
- **模型文件**：
  - 最佳模型权重（best.pt）
  - 最后一个epoch权重（last.pt）
- **训练结果**：
  - 训练曲线图（results.png）
  - 混淆矩阵（confusion_matrix.png）
  - 验证结果图像
  - 训练日志（results.csv）

### 6.3 数据格式
- **图像格式**：JPG、PNG
- **标注格式**：
  - COCO格式：JSON文件，包含images、annotations、categories
  - YOLO格式：TXT文件，每行`class_id center_x center_y width height`（归一化坐标）
- **配置文件格式**：YAML

## 7. 安全需求

### 7.1 数据安全
- 训练数据应妥善保管，避免泄露
- 模型权重文件应备份

### 7.2 系统安全
- 输入验证：对用户输入进行验证，防止恶意文件
- 异常处理：完善的异常处理机制，避免程序崩溃

## 8. 其他需求

### 8.1 可维护性
- 代码应具有良好的可读性和注释
- 模块化设计，便于维护和扩展
- 详细的文档说明

### 8.2 可扩展性
- 支持扩展到多类别检测
- 支持其他模型改进方案
- 支持其他数据集格式

### 8.3 可移植性
- 支持Windows和Linux系统
- 依赖库版本明确，便于环境复现

## 9. 验收标准

### 9.1 功能验收
- [ ] 数据格式转换功能正常工作
- [ ] 数据集验证功能能够发现常见问题
- [ ] P2检测层成功集成并验证
- [ ] BiFPN成功集成
- [ ] 训练流程能够正常运行
- [ ] 模型验证功能正常
- [ ] Web应用能够正常使用

### 9.2 性能验收
- [ ] 训练速度满足要求
- [ ] 推理速度满足要求
- [ ] 模型精度达到预期（mAP50 > 40%）

### 9.3 质量验收
- [ ] 代码质量良好，有清晰注释
- [ ] 文档完整，包括使用说明和技术文档
- [ ] 异常处理完善

---

**文档版本**：v1.0  
**创建日期**：2025年  
**最后更新**：2025年
