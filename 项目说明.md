# 基于YOLOv12的目标检测模型优化项目

## 项目概述

本项目是基于YOLOv12（You Only Look Once version 12）目标检测框架的模型优化项目，针对特定目标（PT）进行检测模型的训练与优化。YOLOv12是2025年发布的最新YOLO系列模型，采用注意力机制设计，在保持实时检测速度的同时，显著提升了检测精度。

项目主要完成了以下工作：
1. **数据处理**：实现了COCO格式到YOLO格式的数据转换，建立了包含1515张训练图像和171张验证图像的数据集
2. **模型改进**：在YOLOv12s基础上集成P2小目标检测层和BiFPN特征融合网络，提升了检测性能
3. **训练优化**：开发了自动化训练脚本，实现了数据集验证、缓存管理、参数调优等功能
4. **应用开发**：提供了基于Gradio的Web界面，支持图像和视频的目标检测
5. **实验验证**：完成了5个版本的训练实验（v1-v5），验证了不同改进方案的效果

项目采用Python语言，基于PyTorch深度学习框架和Ultralytics YOLO库进行开发，完成了从数据处理到模型训练、验证、应用的完整流程。

## 附录文档说明

本项目按照软件工程标准流程，从项目规划到最终交付，形成了完整的项目文档体系。附录1至附录7分别对应项目开发的不同阶段，构成了从计划、需求、设计、实现、测试到使用和总结的完整文档链。

### 附录1 项目开发计划
**定位**：项目立项和整体规划文档，为项目实施提供时间表和任务分解。

**主要内容**：包括项目背景与目标（基于YOLOv12的目标检测模型优化）、项目范围与预期成果（数据处理、模型改进、训练优化、Web应用等）、12周开发计划的时间安排（7个开发阶段：需求分析、概要设计、详细设计、编码实现、测试、文档编写、交付验收）、任务分工与角色职责、风险识别与应对策略（如数据质量、训练时间、硬件资源等风险及解决方案）、质量保证措施等。该文档为整个项目提供了清晰的路线图和组织框架。

### 附录2 软件需求说明书
**定位**：明确系统"要做什么"的文档，是开发与测试的共同依据。

**主要内容**：包括系统总体描述（系统目标、使用场景、用户角色）、功能需求（数据格式转换、数据集验证、模型改进、模型训练、模型验证、Web应用等6大功能模块的详细需求描述）、性能需求（推理速度、检测精度mAP、训练时间等指标要求）、约束条件（运行环境、依赖库、硬件要求）、接口与数据格式（输入输出格式、配置文件格式、API接口定义）等。该文档明确了系统的功能边界和验收标准，为后续设计和开发提供了明确的方向。

### 附录3 概要设计说明书
**定位**：从整体结构上说明"系统怎么分块"的文档，描述系统架构和模块划分。

**主要内容**：包括系统总体架构（采用分层架构：应用层、业务逻辑层、框架层、数据层）、模块划分与关系（数据处理模块、模型改进模块、训练管理模块、模型验证模块、Web应用模块等5大模块的职责和交互关系）、关键技术方案（YOLOv12模型选择、P2检测层设计、BiFPN特征融合网络等）、运行流程（从用户启动到完成训练/推理的整体流程）等。该文档从宏观层面描述了系统的组织结构和各模块之间的关系，为详细设计奠定了基础。

### 附录4 详细设计说明书
**定位**：面向实现层面的详细设计文档，描述各模块的具体实现方案。

**主要内容**：包括各模块的详细设计（数据加载类、训练循环、损失函数、推理后处理等核心组件的设计思路）、主要数据结构与类的设计（类/函数的职责划分、参数和返回值说明）、关键算法流程（坐标转换算法、文件匹配验证、缓存清理、P2检测层验证等算法的详细流程和伪代码）、与外部组件的交互细节（配置文件读写、日志记录、模型权重文件管理等）等。该文档为编码实现提供了详细的技术指导，确保开发人员能够按照统一的设计方案进行实现。

### 附录5 测试分析报告
**定位**：记录测试过程与结果，验证系统是否达到预期要求的文档。

**主要内容**：包括测试环境（硬件配置、软件环境、依赖库版本）、测试用例与测试方法（17个测试用例，涵盖功能测试、性能测试、鲁棒性测试等）、测试结果与指标（mAP、Precision、Recall、F1、FPS等性能指标，各功能模块的测试结果）、缺陷与问题记录（发现的问题、重现步骤、解决情况）、测试结论（是否满足需求、是否可以交付）等。该文档客观记录了系统的测试情况，为系统质量评估和问题修复提供了依据。

### 附录6 操作手册
**定位**：面向使用者的"使用说明书"，提供系统的操作指导。

**主要内容**：包括环境准备步骤（Python环境安装、依赖库安装、CUDA配置等）、项目运行方式（如何启动训练脚本、如何进行推理、如何加载自定义数据集等）、常见参数说明（训练超参数、模型选择、数据路径配置的解释）、结果查看与导出（日志查看、可视化结果、模型权重文件位置等）、常见问题解决方法（显存不足、路径错误、依赖冲突等问题的排查和解决）等。该文档帮助用户快速上手使用系统，提高系统的可用性。

### 附录7 项目开发总结报告
**定位**：对整个项目开发过程的回顾与反思，总结项目成果和经验教训。

**主要内容**：包括项目整体回顾（完成的主要工作、是否达到预期目标）、经验与收获（对YOLOv12模型、数据处理、训练调参、工程化实践等方面的技术心得和项目管理经验）、问题与不足（遇到的技术难点、协作问题、时间管理问题，以及目前尚未完善的地方）、改进与展望（未来可以优化的方向，如模型结构进一步优化、数据扩充、部署到更多平台、性能优化等）等。该文档总结了项目的价值，为后续项目提供了宝贵的经验和参考。

## 项目交付清单

### 源代码
- **提供方式**：电子版本
- **包含内容**：
  - 数据处理脚本：`convert_coco_to_yolo.py`、`check_dataset.py`
  - 训练脚本：`train_yolov12s.py`
  - 模型验证脚本：`test_yolov12_p2.py`、`verify_p2_detection.py`、`final_verify_p2.py`
  - Web应用：`app.py`
  - 配置文件：`PTDataset.yaml`、模型配置文件
  - 依赖配置：`requirements.txt`

### 项目文档
- **提供方式**：纸质版本 + 电子版本
- **包含内容**：
  - 附录1：项目开发计划
  - 附录2：软件需求说明书
  - 附录3：概要设计说明书
  - 附录4：详细设计说明书
  - 附录5：测试分析报告
  - 附录6：操作手册
  - 附录7：项目开发总结报告
  - 附录8：部分核心算法代码
  - 其他文档：`README.md`、`训练说明.md`、`训练问题修复说明.md`、`实习终期报告.md`

### 训练结果
- 5个版本的模型权重文件
- 训练曲线和验证结果
- 性能评估报告

---

**项目完成时间**：2025年  
**文档版本**：v1.0

